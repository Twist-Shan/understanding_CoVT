# 新几何场景的冻结状态验证

200 张新图像上，沿用旧校准状态的深度读出得到 199/200 正确、1 次弃权；专家读出为 198/200 正确、2 次弃权。没有观察到非弃权的方向判断错误。这说明固定状态在本批几何变化下仍能支持高准确率的深度关系读出。

**记录状态**：两个阶段均已完成，远程输出为 EXIT_CODE 0。40 张原生对照中 39 张合格；唯一失败样本直接输出 B，没有生成 depth pad，因此无法进行原生深度读出，并非 cached/uncached 数值不一致。结果已下载并通过 SHA-256 校验。

## 设置

计划见 [固定方案](shift_validation_plan.md)。Colab A100，seed=1901，50 个场景族、200 张图像，包含尺寸变化、椭球、盒体、部分遮挡和相机变化，每类 10 个族、40 张图像。全部新场景用于评估。

固定均值和固定示例来自上一轮八个校准样本，校准方向、尺度和弃权阈值均保持不变。专家和解码器采用 FP32，在 Colab GPU 上运行。问题为 closer to the camera，字母为 24 px；该批次同时改变问法、标记与几何。代码与几何检查在 Colab 上通过：26 passed，8.14 秒。

## 冻结读出结果

| 评估范围 | 专家正确数 | 固定均值正确数 | 固定示例正确数 |
|---|---:|---:|---:|
| 尺寸变化，40 张 | 39 | 40 | 40 |
| 椭球，40 张 | 39 | 39 | 39 |
| 盒体，40 张 | 40 | 40 | 40 |
| 部分遮挡，40 张 | 40 | 40 | 40 |
| 相机变化，40 张 | 40 | 40 | 40 |
| 全部，200 张 | 198 | 199 | 199 |

各列未计为正确的样本均为弃权。固定均值与固定示例的正确率为 99.5%，按场景族 bootstrap 的 95% 区间为 [98.5%, 100%]；专家正确率 99%，区间为 [97.5%, 100%]。区间重叠，不据此声称固定状态读出优于专家。

## 解释范围

### 已完成的原生配对对照

40 张尝试中，39 张通过检查；`shift_00001_label_swap` 没有生成四个 depth pad，保留为不合格样本。语言答案在全部样本中正确 22/40，在合格样本中正确 22/39。

原生、固定均值、固定示例和跨图状态条件的族平均深度正确率均为 96.67%，95% 族 bootstrap 区间为 [90%, 100%]。这与逐图正确率不同：一个族仅有三个合格样本。固定均值与原生条件的逐样本预测完全一致；平均绝对归一化 margin 差为 0.00459。token 打乱条件的族平均正确率为 56.5%，区间为 [48%, 64.5%]。

几何核对发现，“投影面积更大即更近”规则在全部 200 张上正确率为 84%，在预选原生子集上为 92.5%。因此下一轮应预先平衡大小线索一致和冲突条件，而不能仅扩大同类样本后宣称排除了大小捷径。现有实验未检验状态对语言答案的因果作用。

归档文件 `covt-answer-shift-reports-20260918.zip` 为 7,034,801 字节，SHA-256 为 `b073c761a309e10e1827470417f50157ae9be9bc814f0abc3c3cfa5dc80906f1`。包含 JSON/JSONL、图像、隐藏状态、代码与日志；不含专家特征、几何 NPZ 和深度地图 NPY，不能视为完整缓存备份。

本批 200 张图像不需要生成各自的 CoVT 状态，即可由旧固定状态与当前图像的专家特征取得上述结果。因此，正确的原生结构深度图本身不能证明当前图像的 thought 状态携带了决定关系的特有信息。

该阶段没有直接比较全部 200 张图像的原生状态，也没有重算干预后的语言答案。对原生状态的配对比较仅限预先选定的 40 张图像中的 39 张合格样本。渲染场景仍是简单几何体，不能推及自然图像或其他感知通道。

远程目录：`/content/covt-pilot/runs/shift-validation-20260918-050713`。逐行记录已归档到 `runs/colab-20260918-answer-shift/extracted/`。
