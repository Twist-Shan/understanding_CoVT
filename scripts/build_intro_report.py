"""Rebuild the standalone report with an explanation-first main text."""
from pathlib import Path
import runpy
import re
import json

ROOT = Path(__file__).resolve().parents[1]
base = runpy.run_path(str(ROOT/'scripts/build_html_report.py'))
old = base['document']
render = base['render_block']
sections = base['sections']

def fig(section):
    return ''.join(render(kind,args) for kind,args in sections[section-1]['blocks'] if kind=='figure')

def table(head, rows):
    return render('table',[head,rows])

chapters=[]
def add(title, body):
    chapters.append((title,body))

add('CoVT 是什么？我们研究的是它的哪一部分？', '''
<p><strong>CoVT 是一个能够看图并用文字回答问题的模型。</strong>我们实际使用的是公开的深度版本 <code>Wakals/CoVT-7B-depth</code>。它建立在 Qwen2.5-VL 视觉语言模型上。这里的“视觉语言模型”，就是能够同时接收图片和文字问题、再生成文字回答的模型。</p>
<p>例如，给它一张标了 A、B 的图片，问“哪个点离相机更近？”，我们希望它最后回答 A 或 B。这个任务只比较远近顺序，不要求模型说出具体距离是多少米。</p>
<p>CoVT 的特别之处在于：<strong>除了生成文字，它还有专门用于深度信息的中间位置。</strong>在我们检查的正常输出里，模型会生成四个深度标记。这些标记本身不是四张图片，也不是四句话。每个位置在模型内部对应一组数值；我们把这组数值称为“状态”。</p>
<p>我们希望弄清楚：这些被称为深度状态的数值，到底是否提供了当前图片的远近信息，以及模型最后回答 A 或 B 时是否真的用到了它们。</p>
<aside class="example"><strong>先记住三个对象</strong><p>① 输入图片和问题；② 模型内部的四个深度状态；③ 最后生成的文字答案。后面还会出现“深度图”，它是另外计算出来的可视化结果。</p></aside>
<p class="conclusion"><strong>这一节的结论：</strong>我们研究的是一个已经训练好的公开模型。我们没有重新训练 CoVT，而是在检查它原有的深度状态、深度图和文字答案之间是什么关系。</p>
''')

add('深度图怎样得到？为什么图对了，答案还可能错？', '''
<p>“深度图”是一张与输入图片位置对应的数值图。每个位置有一个分数，可以用颜色显示出来。我们在 A、B 附近分别取分数，判断哪一点更近。这里的分数没有换算成米，因此不能把颜色条当成实际距离。</p>
<p><strong>一个关键细节是：CoVT 的深度图需要两种输入共同计算。</strong>第一种是前面说的四个深度状态；第二种来自一个独立的深度模型 Depth Anything V2。后者专门处理输入图片，提供图像中各位置的特征。报告里把它简称为“深度专家”。</p>
<div class="flow"><div><b>文字回答</b><p>图片 + 问题 → CoVT 的逐步计算 → 最后回答 A 或 B</p></div><div><b>深度图</b><p>CoVT 的四个深度状态<br>＋ 同一图片的深度专家特征<br>↓<br>原模型的解码器 → 一张深度图</p></div></div>
<p>“解码器”在这里指把上述两种数值转换成深度图的计算模块。我们说“恢复原生解码器”，就是找回原模型公开的参数、按原方法重建这个模块，并检查输出是否一致。</p>
<p>这样就有了一个必须区分的问题：<strong>深度图正确，可能主要是深度专家提供的图像信息足够好。</strong>仅看这张图，还无法知道四个状态贡献了多少；也无法知道生成文字答案的计算是否使用了这些状态。</p>
<p>下面是一张实际实验图片。左边是输入，中间是恢复的深度图，右边是深度专家自身的结果。B 比 A 更近；两张深度图都判断正确，但 CoVT 的文字答案是 A。</p>
'''+fig(6)+'''
<p class="conclusion"><strong>这一节的结论：</strong>“深度图能看出谁更近”和“文字答案能答对”需要分别测量。后面的实验就是先检查深度图由什么决定，再检查深度状态能否改变文字答案。</p>
''')

add('我们的实验具体在做什么？图片、答案和比较方法', '''
<h3>我们想回答的三个问题</h3><ol><li>原模型的深度图能否被可靠地恢复出来？</li><li>每张图是否必须使用自己的四个深度状态，才能得到正确深度图？</li><li>把深度状态改成另一张图的状态，会不会改变最后的文字答案？</li></ol>
<h3>为什么先用合成图片？</h3><p>我们自己生成球体、盒体等简单场景，并在物体上标 A、B。生成场景时，物体的位置和表面深度是已知的，因此可以明确知道正确答案。这样也能控制变化：只换字母、换颜色，或者按设定交换两个物体的远近。</p>
<p>同一个基础场景派生出的几张图片叫一个“场景族”。例如原图、交换远近的图、只换颜色的图、交换 A/B 字母的图可以属于同一族。这些图很相似，所以我们不会把它们都看成完全独立的新场景。</p>
<h3>怎样判断实验结果？</h3>
'''+table(['要测量的东西','怎样判断','简单例子'],[
['深度图是否正确','在 A、B 周围取图上的分数，按预先固定的规则判断远近，再与真实标签比较。','真值 B，图也判 B，就记为深度正确。'],
['文字是否答对','解析最后的 A/B 答案，与真实标签比较。','真值 B，文字答 A，就记为文字错误。'],
['改状态后答案是否变化','让模型重新生成后续回答，比较改动前后的有效答案。','原来答 A，改后明确答 B，才是一次答案翻转。'],
['改动是否只影响了格式','检查是否完整结束、有没有重复标签或同时写 A 和 B。','输出 A or B 不能算成明确答 B。']])+'''
<p>为了确定深度分数的正负方向，我们单独留出校准图片。可以把“校准”理解为先规定分数怎样转换成 A/B 判断；规定后再去评价另一批图片，不能看到测试答案后反过来改规则。分数差太小时记为“弃权”，表示没有作出明确选择。</p>
<p>所有模型实验都在 Colab A100 上完成，模型参数保持不变。下面各轮的数据不同，必须看各自的分母；例如 8/32 与 25/32 并不是同一批题目前后的成绩。</p>
<p class="conclusion"><strong>这一节的结论：</strong>我们的实验是小规模、可控制的机制检查。它能帮助区分几种解释，但还不能代表模型在所有真实图片上的表现。</p>
''')

add('实验一：先恢复深度图，检查计算是否可靠', '''
<h3>目的</h3><p>如果代码恢复错了，或者相同输入换一种计算方式就答得不同，后面的状态替换很难解释。因此先检查恢复流程。这一组检查在原记录中叫 E0。</p>
<h3>设计与内容</h3><ol><li>从公开模型中找出深度解码器所需参数，按原计算方法重建，并用相同数值核对结果。</li><li>先生成 12 张图，检查模型是否自然生成四个深度标记，以及保存后重新计算能否得到同样结果。</li><li>比较“复用前面计算的缓存”和“重新计算整个前缀”两种生成方式。这两种方式应当在本实验中给出一致答案。</li><li>首轮发现差异后，用 4 张图比较不同数值精度。完整使用 FP32 后，这 4 张图不再出现差异。随后再用新生成的 40 张图验证。</li></ol>
<p>BF16、FP16、FP32 是保存和计算数值的不同精度。这里不需要记住具体位数，只需要知道：我们提高了整个语言模型的数值精度，以减少本批观察到的计算差异，没有重新训练模型。</p>
<h3>结果</h3>
'''+table(['阶段','检查结果','深度与文字结果'],[
['首轮：12 张图','10 张通过全部检查；2 张的缓存与无缓存答案不同。','合格评估图共 7 张：深度 7/7，文字 2/7。'],
['完整 FP32：40 张新图','40/40 通过；其中 8 张用于校准，32 张用于评估。','32 张评估图：深度 32/32，文字 8/32。']])+'''
<h3>怎样理解</h3><p>在后续的 32 张图中，有 24 张“深度图判断对、文字答错”。前面那张 B 更近但文字答 A 的图片，就是这种情况的一个首轮真实例子。两批图片不同，因此不能用首轮和后续的准确率差来估计提高精度带来的收益。</p>
<p class="conclusion"><strong>当前结论：</strong>原生深度分支已恢复，完整 FP32 流程在本批检查中可用。深度图与文字答案的表现明显不同，差异来自哪里还需要后续实验。</p>
''')

add('实验二：深度图主要依靠哪种输入？', '''
<h3>目的</h3><p>深度图同时使用 CoVT 状态和深度专家特征。我们要分别改变这两种输入，看哪一种更能改变图里的远近判断。这一组实验在原记录中叫 E1。</p>
<h3>设计与内容</h3><p>准备一对图片：原图和交换远近后的图。两张图分别运行，保存它们各自的四个状态和专家特征。然后按下表重新组合。这里只重新计算深度图，没有重新生成文字答案。</p>
'''+table(['组合','CoVT 的四个状态取自','专家特征取自','要检查什么'],[
['原组合','原图','原图','作为比较基准。'],['只换状态','交换远近后的图','原图','状态改变能否改变深度判断？'],['只换专家特征','原图','交换远近后的图','图像特征改变能否改变深度判断？'],['两者都换','交换远近后的图','交换远近后的图','与完整换图后的读出比较。']])+'''
<h3>简单例子</h3><p>假设原图是 B 更近，交换后是 A 更近。如果只换四个状态，读出的图就由 B 更近变成 A 更近，说明这些状态在该条件下足以改变深度判断。如果只有换专家特征才改变判断，就需要重点考虑专家特征的作用。</p>
<h3>实际结果</h3><p>扩展到 8 个场景族后，只换状态时，<strong>0/8</strong> 个场景的深度判断改变；只换专家特征时，<strong>8/8</strong> 个场景改变。关系分数也显示：换状态平均只变化约 0.000311，换专家特征约变化 1.802808。这里的数值是校准后的分数变化，不是距离变化。</p>
<p>下图展示首轮的一个真实场景。每一行改变状态来源，每一列改变专家特征来源。观察上下两图与左右两图的差别，就能看到这一对照的含义。</p>
'''+fig(9)+'''
<p class="conclusion"><strong>当前结论：</strong>在这些深度读出对照中，专家图像特征的改变影响更大。这个实验没有测试文字答案是否使用四个状态，因此还不能回答最后的语言推理问题。</p>
''')

add('实验三：如果每张图都用同一组状态，深度图还对吗？', '''
<h3>目的</h3><p>上一轮换状态几乎没有改变深度图。于是继续问：是否连“每张图都生成自己的状态”这一步都不一定需要？</p>
<h3>设计与内容</h3><ol><li>从校准图片中取四个状态，计算平均值。之后所有测试图都使用这同一组平均状态，各图仍保留自己的专家特征。</li><li>另外比较：取一个校准样本的状态、取别的图片的状态、打乱四个状态的顺序，以及把数值清零。</li><li>先在原来的 32 张测试图上比较，再保持这组固定状态不变，测试 200 张新的几何图片。新图片有尺寸、形状、遮挡和视角变化。</li></ol>
<h3>简单例子</h3><p>图片甲是 A 更近，图片乙是 B 更近。固定状态条件下，两张图用完全相同的四组数值，但专家仍分别处理甲、乙。如果最后还能得到不同且正确的判断，就说明图像差异可以通过专家特征进入深度图。</p>
<h3>结果</h3>
'''+table(['条件','结果','需要注意'],[
['原 32 张：用自己的状态','32/32 正确','原始比较基准。'],['原 32 张：固定均值、固定示例、跨图状态','各条件均 32/32 正确','并不意味着数值图完全相同。'],['原 32 张：打乱四个状态顺序','平均正确率 58.125%','随机重复 5 次，独立场景族仍只有 8 个。'],['原 32 张：清零状态或投影后的向量','全部弃权','没有形成明确的远近选择。'],['新 200 张：继续用旧固定状态','199 正确，1 弃权','没有用新图重新校准。']])+'''
<p>在新图中，又预选 40 张生成各自的自然状态，其中 39 张获得四个状态；这些合格图上，固定与自然状态的深度预测一致。说明固定状态的现象不只出现在最初的 32 张图上。</p>
<p class="conclusion"><strong>当前结论：</strong>当前图片专属的末端状态，并未在这些测试中显示出不可替代的作用。但顺序打乱和清零会影响读出，所以也不能说这四个位置完全没有作用。</p>
''')

add('实验四：文字答错，会不会只是题目没问清或字母没看清？', '''
<h3>目的</h3><p>在解释模型内部机制前，先排查更直接的原因：模型是否认得 A、B？是否理解“近”和“远”？换成直白问法后是否改善？</p>
<h3>设计与内容</h3><p>取同一批 16 张图，分别让 CoVT 和它的 Qwen 基座模型完成 8 类任务，包括原技术问法、直接问谁更近或更远、识别左边字母、比较物体大小、回答左右，以及放大字母后的任务。每类任务都使用这 16 张图，避免换题后再比较成绩。</p>
<p>还检查成对一致性。例如只交换 A/B 字母、物体不动：更近的物体没变，但正确字母应从 B 变为 A。如果问的是“左边还是右边”，答案则应该保持不变。</p>
<h3>结果与例子</h3><p>两模型的字母识别和大小比较都答对 <strong>16/16</strong>。直接问谁更近时都答对 <strong>11/16</strong>；CoVT 原技术问法只答对 <strong>6/16</strong>。真实案例中，CoVT 曾在原问法下答 A，改成“谁离相机更近”后答 B，而该图正确答案就是 B。</p>
<p>不过，交换标签后，两模型都只有 <strong>3/8 对</strong>按预期改变近距离字母答案。下面是一对表现一致的真实例子，不代表所有图都一致。</p>
'''+fig(13)+'''
<p class="conclusion"><strong>当前结论：</strong>问法确实会影响答案；字母看不清无法单独解释错误。即使简单识别任务通过，远近关系回答仍可能不稳定，当前也没有显示 CoVT 在直白问近任务上优于基座。</p>
''')

add('实验五：会不会只是看哪个物体更大？', '''
<h3>目的</h3><p>在简单场景中，离相机近的物体往往看起来更大。如果只选较大的物体就能答对，较高正确率并不能充分说明模型在判断深度。我们需要把大小和远近分开安排。</p>
<h3>为什么做这个对照？</h3><p>先在前面的 200 张新图上测试一个简单规则：“谁占的像素多，就选谁更近。”这个规则已经能答对 84%；在预选的 40 张图里能答对 92.5%。因此，大小线索确实需要控制。</p>
<h3>设计与内容</h3><p>新生成 32 张图。一半让更近的物体看起来更大，称为“一致”；另一半让更近的物体反而更小，称为“冲突”。A 更近与 B 更近也各占一半。每个基础场景做四种组合：</p>
'''+table(['','A 看起来更大','B 看起来更大'],[['A 实际更近','一致：选大的会答对','冲突：选大的会答错'],['B 实际更近','冲突：选大的会答错','一致：选大的会答对']])+'''
<p>我们通过调整物体实际半径来控制图像里的大小。因此，这个设计平衡了表观大小，但同时会改变真实半径和地面接触位置，仍有其他线索没有完全隔离。</p>
<h3>结果</h3>
'''+table(['结果','全部 32 张','其中大小冲突的 16 张'],[['CoVT 文字答案','25/32','13/16'],['固定状态的深度图','30/32','14/16'],['深度专家自身的图','26/32','10/16'],['总选较大物体的规则','16/32','0/16']])+'''
<p>下图是同一场景的四种实际组合。其中一张 A 更近但看起来更小，CoVT 和固定状态深度图仍判断 A；因此，该例不能由“总选大物体”解释。</p>
'''+fig(16)+'''
<p class="conclusion"><strong>当前结论：</strong>大小线索不足以解释全部正确结果，固定状态在冲突组也仍有较高正确率。不过这只是 32 张新的合成图片，与前面的 8/32 语言成绩不能当作同一批题目的改进。</p>
''')

add('实验六：直接换掉内部状态，看文字答案会不会变', '''
<h3>目的</h3><p>前面的状态替换只用于重算深度图。现在要直接改变语言模型内部的计算，然后让它继续生成文字，检查答案是否受影响。</p>
<h3>先说明两个词</h3><p>模型有 28 个连续处理层，每一层都会更新各个位置的数值。“换一层”是只在某一步替换状态；“换一个位置”是只替换该层中特定 token 对应的数值。四个深度标记有四个位置，两种数量可以分别扩大。</p>
<h3>设计与内容</h3><ol><li>选一张接受修改的目标图，再选一张远近关系相反的图，后者提供替换状态，称为供体。例如目标图 A 更近，供体图 B 更近。</li><li>先在第 14 层，把目标的四个深度位置替换为供体的对应数值。目标输入图片与问题保留原样，从深度标记之后重新生成文字。</li><li>把目标自己的数值原样放回去，检查答案能否完全复现。这是验证替换代码的对照。</li><li>再扫描第 0、4、8、14、20、24、27 层，并增加置零条件。每次都检查重新生成的完整文本。</li></ol>
<p>平衡数据共有 32 张，但只有 26 张自然生成了四个深度标记。能找到所需相反深度配对的目标有 20 张，来自 6 个场景族。因此，后续“0/20”只适用于这个筛选后的集合。</p>
<h3>我们原本希望看到什么？</h3><p>说明性例子：目标原来答 A。换入另一张 B 更近图片的状态后，若它稳定、明确地改答 B，就可以说明所换状态在这些条件下能够影响答案。还要与原样写回、同深度供体等条件比较，排除一般数值扰动的影响。</p>
<h3>实际结果</h3><p>第 14 层的相反深度替换为 <strong>0/20 答案翻转</strong>；扫描的七个层也全部为 <strong>0/20</strong>。数值确实发生了替换，原样写回检查也通过，因此不能把结果简单归因于替换代码没执行。</p>
<p>置零会让一些输出发生很大变化，但常见情况是立即结束、重复标签或输出“A or B”。这些结果没有形成明确、格式正常的相反答案。我们因此把输出损坏与有效答案翻转分开统计。</p>
<p class="conclusion"><strong>当前结论：</strong>已扫描的单层四位置替换没有改变答案。这个结果还留下一个问题：是否替换的层数或位置数太少？下一轮专门检查这一点。</p>
''')

add('实验七：扩大到全部层和更多位置，结果是否不同？', '''
<h3>目的</h3><p>检验你提出的疑问：单层替换可能不够；只换四个深度标记，也可能覆盖不到足够的信息。</p>
<h3>设计与内容</h3><p>沿用同一组 20 张图，依次扩大范围，保持每阶段的目标图片一致。下面的层号从 0 开始：</p>
'''+table(['阶段','在哪些层替换','每层替换哪些位置'],[['前部层','0–8，共 9 层','四个深度标记'],['中部层','9–17，共 9 层','四个深度标记'],['后部层','18–27，共 10 层','四个深度标记'],['全部层','0–27，共 28 层','四个深度标记'],['全部层、更多位置','0–27，共 28 层','前面 11 个生成位置，加四个深度标记，共 15 个']])+'''
<p>每个阶段都比较相反深度供体和同深度供体。同深度供体来自其他场景族，并匹配标签与大小、远近条件，用来观察一般跨图替换会造成多大变化。每一步生成时都会重新施加指定替换。</p>
<p>前四阶段均无有效翻转后，按预定规则才进入 15 位置阶段。这里没有替换输入图像的位置、问题文字的位置，也没有复制供体的答案。因此“全层”只表示每一层都动了指定位置，并不表示整个模型的所有位置都被替换。</p>
<h3>结果</h3><p>5 个阶段、2 种供体、20 张目标，共 <strong>200 次干预</strong>。全部正常结束，格式有效，<strong>不但 A/B 答案没变，后续生成的整段 token 序列也没有变化</strong>。100 次原样写回检查全部通过。</p>
<p>答案没变时，我们进一步看模型对 A/B 的相对概率有没有细微移动。下图展示每阶段 20 张图中的最大变化。纵轴单位是“百分点”；蓝色表示相反深度供体，橙色表示同深度供体。这里画的是最大值，不是平均收益。</p>
'''+fig(20)+'''
<h3>一个真实例子</h3><p>变化最大的案例中，目标图原本答 A，供体图原本答 B。全层 15 位置替换后，目标对 A 的条件概率从约 <strong>84.3427%</strong> 降为 <strong>84.2955%</strong>，只下降约 <strong>0.0472 个百分点</strong>，最后仍答 A。全部 20 个目标中，有 6 个使用了本来就回答相反字母的供体；这 6 个也没有翻转。</p>
<p>同深度供体的最大概率变化约 0.0422 个百分点，也很小。按场景族比较两类供体的定向影响，差值区间包含零，因此本批没有得到稳定的“往相反深度答案移动”的证据。</p>
<p class="conclusion"><strong>当前结论：</strong>把范围扩大到全 28 层和 15 个生成位置后，答案仍没有改变。单层范围小已不足以单独解释结果，但其他位置是否承载答案所需信息，以及所选供体是否提供了足够有效的差异，仍需验证。</p>
''')

add('把所有结果放在一起，目前到底能说什么？', '''
'''+table(['问题','目前结果','可以得出的结论'],[
['原来的深度图能恢复吗？','已恢复；完整 FP32 的 40 张检查全部通过。','在当前固定版本和场景中可以可靠运行。'],
['深度图正确，是否说明四个状态包含了每张图必需的深度信息？','固定状态在 32 张上全对，在新 200 张上正确 199 张。','这种必要性尚未得到证明，专家特征的贡献很大。'],
['高正确率是否全靠物体大小？','大小冲突时，固定状态深度仍为 14/16。','大小线索无法解释全部结果，但其他线索尚未完全排除。'],
['替换这些状态能控制文字答案吗？','单层、多层、全层和 15 位置替换都没有有效翻转。','当前替换方法未实现稳定答案控制。'],
['可以说 CoVT 完全没用视觉思考吗？','仅检查了一个深度版本、小规模合成场景和指定位置。','不能推广到全部信息路径、其他任务或全部模型。']])+'''
<p><strong>最值得保留的观察是：深度图的正确性与文字答案的正确性可以分开。</strong>在恢复后的解码器中，固定状态加当前图片的专家特征已经能够生成很多正确的深度关系；与此同时，把若干内部状态换掉，并没有改变后面的文字答案。两类现象需要分别解释。</p>
<h3>下一步应该检查什么？以下尚未执行</h3><p>先检查目前没有覆盖的位置：深度标记之后、正式答案之前的过渡位置，问题文字位置，以及图像位置。还需要验证供体与目标在拟替换位置上是否确实存在与远近判断有关的差别，并保留完整生成和严格格式检查。</p>
<p>如果能找到一种可重复的方法，让模型在保持正常输出的同时按预期改变远近答案，再讨论怎样修复或训练会更有依据。目前还没有得到这样的控制方法。</p>
<p class="conclusion"><strong>总的结论：</strong>原生深度图恢复成功；深度图对当前图片专属末端状态的依赖很弱；已测试的内部状态替换没有控制文字答案。这些是当前实验支持的范围，具体机制原因仍未查明。</p>
''')

toc=''.join(f'<a href="#chapter{i}"><span>{i:02d}</span>{title}</a>' for i,(title,_) in enumerate(chapters,1))+'<a href="#appendix">附录：完整数值、计算方法与证据</a>'
body=''.join(f'<section id="chapter{i}"><div class="section-number">{i:02d}</div><h2>{title}</h2>{content}</section>' for i,(title,content) in enumerate(chapters,1))
appendix=''.join(base['parts'])
header='''<header><div class="eyebrow">CoVT 深度实验 · 从头解释</div><h1>CoVT 是什么？<br>我们做了什么实验，又看到了什么？</h1><p class="meta">基于截至 2026 年 9 月 18 日的已完成记录 · 可离线阅读</p><p>这份报告从模型的输入和输出开始讲。先理解深度图和文字答案的区别，再看每一步为什么要做、具体改了什么，以及结果能说明什么。正文不要求预先了解状态替换、token 或 E0/E1。</p><p><strong>先给结论：</strong>我们恢复出了原模型的深度图，但深度图判断正确时，文字答案仍可能错误。进一步替换指定内部状态，也没有让文字答案按预期改变。下面逐步解释这三个发现是怎样得到的。</p><div class="actions"><button id="expand" type="button">展开全部附录</button><button id="collapse" type="button">收起附录</button><button id="print" type="button">打印 / 保存为 PDF</button></div></header>'''
document=old[:old.index('<body')]+f'''<body id="top"><a class="skip" href="#main">跳到正文</a><div class="layout"><nav aria-label="报告目录"><h2>从这里开始阅读</h2>{toc}</nav><main id="main">{header}{body}<section id="appendix"><h2>附录：完整数值、计算方法与证据</h2><p>正文已经给出实验逻辑与主要结果。需要核对指标、版本、原始记录或更多图表时，再展开这一部分。附录沿用原报告编号，与正文的十一节编号分别使用。</p><details><summary>展开完整技术记录与全部 13 张原始图表</summary>{appendix}</details></section><footer>本次只重写报告，没有运行新的模型实验。所有图片取自已有归档；说明性例子与真实案例分别标注。<br><a href="#top">回到顶部 ↑</a></footer></main></div>'''+old[old.index('<script>'):]
document=document.replace('</style>','.flow{display:grid;grid-template-columns:1fr 1fr;gap:18px;margin:24px 0}.flow>div{background:#eef5f7;border:1px solid #dce4e9;padding:22px}.flow p{margin-bottom:0}code{overflow-wrap:anywhere;font-size:.9em}#appendix details section{padding:18px 8px;border:0;border-bottom:1px solid #dce4e9} @media(max-width:760px){.flow{grid-template-columns:1fr}} </style>')
document=document.replace('<title>CoVT 实验报告 · HTML 详解版</title>','<title>CoVT 是什么：实验设计与结果，从头解释</title>')
target=base['target']
target.write_text(document,encoding='utf-8')
(target.parent/'report_manifest.json').write_text(json.dumps({'html':target.name,'main_chapters':len(chapters),'original_figures':13,'offline':True,'new_model_experiments':False,'builder':'scripts/build_intro_report.py','structure':'CoVT introduction, reconstruction, controlled experiments, results, technical appendix'},ensure_ascii=False,indent=2),encoding='utf-8')
assert len(chapters)==11
print('Rewritten introduction-first report:',target)
